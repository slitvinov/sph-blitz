#include "ParticleContext/particlecontext.h"
/// \file particlecontext.h
/// \brief define default methods for ParticleContext

ParticleContext::ParticleContext() {
  // do nothing as a default 
}

void ParticleContext::AddParticle(const spParticle) {
  // do nothing as a default 
}

void ParticleContext::RemoveParticle(const spParticle) {
  // do nothing as a default
}

ParticleContext::~ParticleContext() {
  // do nothing as a default 
}
