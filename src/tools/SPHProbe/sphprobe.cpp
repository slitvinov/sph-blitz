#include <iostream>
int main() {
  /// read input files
  /// read parameters of the kernel
  ///    cutoff, kernell type
  ///    times t1, t2
  /// create a cell structure  (no boundary)
  /// project to the grid
  /// approximate in time
  /// output 
  return 0;
}
