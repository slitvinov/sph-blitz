#!/usr/bin/awk -f

/LOOP\(/ {
    sub(/*/, "")
    sub(/LOOP/, "LOOP_P", "")
    print
    next
}

{
    print
}

function tab(s)
{
    sub(/[^\t ].*/, "", s)
    return s
}

function trim(s)
{
    sub(/^[\t ]*/, "", s)
    sub(/[\t ]*$/, "", s)
    return s
}
