./configure
make 
mv src/sphopt .
mkdir outdata
awk/sphrmcm.awk cases/test-p.cfg > test-p.cfg

parameter files:
polymer.cfg
test-p.cfg

./start.sh

Marco's cases (see test-conf.cfg, CELLS options)
Ly= 1*10^-6  2 altix
    2*10^-6  4 shige /scratch/files/cpp/sph-opt/outdata/par.nc (done)
    4*10^-6  8 shige
    6*10^-6  12 sachiko /scratch/litvinov/src/sph-opt/outdata/par.nc

Output is in outdata/par.nc
to get a text file 
cdf2punto.sh par.nc 0 

CXXFLAGS='-g -pg' ./configure --with-netcdf=$HOME
./configure --with-netcdf-include=$HOME/include --with-netcdf-libdir=$HOME/lib 
ncks -s "%e\n" -d timestep,0,0 -v R test/par.nc | awk 'n==1; !NF{n=1}' | xargs -n 3 
